# Copy to .env.local and fill AWS_BEARER_TOKEN_BEDROCK with your real key.
# (local dev only; never commit .env.local). ARCADE ORACLE -> Claude on AWS Bedrock (Converse API).
AWS_BEARER_TOKEN_BEDROCK=your-bedrock-api-key
BEDROCK_REGION=eu-north-1
BEDROCK_MODEL_ID=eu.anthropic.claude-haiku-4-5-20251001-v1:0
